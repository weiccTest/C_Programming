/*
	数组操作：
	添加元素、修改元素、删除元素、查看数组的元素
	
	基于数组开发一个随手记的记事本；
	功能：
		1、创建事件(时间、地点、要做的事情)
		2、修改事件(修改时间、地点、要做的事情)
		3、删除事件
		4、查看所有事件(根据日期降序，也可根据用户输入日期查找)
		
	程序数据结构：
	1、数组time保存日期，因为在同一天，用户可以记录很多事情
	2、数组description保存用户事件的详细信息(地点、要做的事情)
*/

#include <stdio.h>

double average(double a,double b){
	return (a+b)/2;
}
int main(void){
	double x, y, z;
	printf("Enter a three number:");
	scanf("%1f%1f%1f",&x,&y,&z);
	printf("Average of %g and %g:%g\n",x,y,average(x,y));
	printf("Average of %g and %g:%g\n",x,z,average(x,z));
	printf("Average of %g and %g:%g\n"y,z,average(y,z));
	
	return 0;
}
